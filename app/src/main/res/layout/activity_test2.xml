<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".features.test.presentation.page.TestActivity">

    <TextView
        android:id="@+id/tvProvider"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Provider text" />

    <Button
        android:id="@+id/btnProvider"
        android:layout_marginEnd="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="get provider"
        app:layout_constraintStart_toEndOf="@id/tvProvider"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:layout_marginTop="12dp"
        android:text="get lazily"
        android:id="@+id/btnLazy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvLazy" />

    <TextView
        android:id="@+id/tvLazy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvProvider"
        tools:text="Lazy Text" />

</androidx.constraintlayout.widget.ConstraintLayout>